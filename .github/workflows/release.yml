name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew build --no-daemon

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            build/libs/tower-defense-*.jar
          body: |
            ## Tower Defense Mod ${{ github.ref_name }}
            
            Mod tower defense 2v2 pour Minecraft 1.21.4 (Fabric).
            
            ### Nouveautés v1.1.0
            - **Mode solo** : joue seul contre l'IA (villageois)
            - IA avec stratégie (income, défense, attaque) et bonus économiques
            - Script `server/start.sh` pour lancer le serveur avec Java 21
            
            ### Installation
            1. Installer [Fabric Loader](https://fabricmc.net/use/) pour Minecraft 1.21.4
            2. Installer [Fabric API](https://modrinth.com/mod/fabric-api)
            3. Télécharger le JAR ci-dessous
            4. Placer le JAR dans `.minecraft/mods/`
          draft: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
